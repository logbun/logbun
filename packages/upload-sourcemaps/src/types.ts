export type Options = {
  apiKey: string;
  release?: string;
  endpoint: string;
};

export type Sourcemap = {
  sourcemapFilename: string;
  sourcemapFilePath: string;
  jsFilename: string;
  jsFilePath: string;
};
