{
  "name": "logbun",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:sdk": "turbo run build --filter=core --filter=js --filter=node --filter=react --filter=nextjs",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@logbun/eslint-config": "workspace:*",
    "@logbun/typescript-config": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
