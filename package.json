{
  "name": "@logbun/root",
  "private": true,
  "scripts": {
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev": "turbo dev",
    "build": "turbo build",
    "apps:dev": "turbo dev --filter='./apps/*'",
    "apps:build": "turbo build --filter='./apps/*'",
    "apps:build:www": "turbo build --filter=root --filter=www",
    "apps:build:app": "turbo build --filter=root --filter=app",
    "apps:build:api": "turbo build --filter=root --filter=api",
    "apps:build:packages": "turbo build --filter=clickhouse --filter=db --filter=eslint-config --filter=tailwind-config --filter=ui --filter=utils",
    "packages:dev": "turbo dev --filter='./packages/*'",
    "packages:build": "turbo build --filter='./packages/*'",
    "packages:build:sdk": "turbo build --filter=core --filter=js --filter=nextjs --filter=react --filter=upload-sourcemaps",
    "packages:publish": "turbo run build lint && changeset version && changeset publish",
    "clickhouse:migrate": "dotenv -- turbo --filter clickhouse migrate",
    "db:studio": "pnpm --filter db studio"
  },
  "packageManager": "pnpm@8.13.0",
  "dependencies": {
    "@changesets/cli": "^2.27.1"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.496.0",
    "@logbun/eslint-config": "workspace:*",
    "@logbun/typescript-config": "workspace:*",
    "prettier": "^3.2.4",
    "semver": "^7.5.4",
    "turbo": "^1.11.3"
  },
  "engines": {
    "node": ">=18"
  }
}
